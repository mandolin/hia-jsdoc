# HIA JSDoc Standalone Example

Runs the HIA JSDoc runner against `src/math.js` and emits native JSDoc HTML, HIA theme metadata, HIA Integration JSON and a producer result manifest.

```sh
hia-jsdoc --config hia-jsdoc.config.json
```
